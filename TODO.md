# TODO

- [ ] Import the repository in Read the Docs and keep configuration set to `.readthedocs.yaml`.
- [ ] Add a documentation badge in `README.md` pointing to the Read the Docs project URL.
- [ ] Add a GitHub Actions workflow that runs `sphinx-build` on every pull request.
